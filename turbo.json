{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "WEAPON_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "WEAPON_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "weapon#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@weapon-ai/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
