---
title: CLI
description: Weapon CLI opcije i naredbe.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

Weapon CLI po defaultu pokreće [TUI](/docs/tui) kada se pokrene bez ikakvih argumenata.

```bash
weapon
```

Ali takođe prihvata naredbe kao što je dokumentovano na ovoj stranici. Ovo vam omogućava programsku interakciju sa Weapon.

```bash
weapon run "Explain how closures work in JavaScript"
```

---

### tui

Pokrenite Weapon terminalski korisnički interfejs.

```bash
weapon [project]
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--prompt`   |        | Prompt za upotrebu                                                       |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--port`     |        | Port na kojem treba slušati                                              |
| `--hostname` |        | Hostname na kojem treba slušati                                          |

---

## Naredbe

Weapon CLI takođe ima sljedeće naredbe.

---

### agent

Upravljajte Weapon agentima.

```bash
weapon agent [command]
```

---

### attach

Priključite terminal na već pokrenut Weapon backend server pokrenut putem `serve` ili `web` naredbi.

```bash
weapon attach [url]
```

Ovo omogućava korištenje TUI-ja sa udaljenim Weapon backend-om. Na primjer:

```bash
# Start the backend server for web/mobile access
weapon web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
weapon attach http://10.20.30.40:4096
```

#### Opcije

| Opcija      | Kratko | Opis                                 |
| ----------- | ------ | ------------------------------------ |
| `--dir`     |        | Radni direktorij za pokretanje TUI-a |
| `--session` | `-s`   | ID sesije za nastavak                |

---

#### create

Kreirajte novog agenta s prilagođenom konfiguracijom.

```bash
weapon agent create
```

Ova naredba će vas voditi kroz kreiranje novog agenta sa prilagođenim sistemskim promptom i konfiguracijom alata.

---

#### list

Navedite sve dostupne agente.

```bash
weapon agent list
```

---

### auth

Naredba za upravljanje vjerodajnicama i prijavom za provajdere.

```bash
weapon auth [command]
```

---

#### login

Weapon pokreće lista provajdera na [Models.dev](https://models.dev), tako da možete koristiti `weapon auth login` da konfigurirate API ključeve za bilo kojeg provajdera kojeg želite koristiti. Ovo je pohranjeno u `~/.local/share/weapon/auth.json`.

```bash
weapon auth login
```

Kada se Weapon pokrene, učitava dobavljače iz datoteke vjerodajnica. I ako postoje neki ključevi definirani u vašim okruženjima ili `.env` fajl u vašem projektu.

---

#### list

Navodi sve autentifikovane dobavljače pohranjene u datoteci vjerodajnica.

```bash
weapon auth list
```

Ili kratka verzija.

```bash
weapon auth ls
```

---

#### logout

Odjavljuje vas s provajdera tako što ga briše iz datoteke vjerodajnica.

```bash
weapon auth logout
```

---

### github

Upravljajte GitHub agentom za automatizaciju repozitorija.

```bash
weapon github [command]
```

---

#### install

Instalirajte GitHub agenta u svoj repozitorij.

```bash
weapon github install
```

Ovo postavlja neophodni tok rada GitHub Actions i vodi vas kroz proces konfiguracije. [Saznajte više](/docs/github).

---

#### run

Pokrenite GitHub agent. Ovo se obično koristi u GitHub Actions.

```bash
weapon github run
```

##### Opcije

| Opcija    | Opis                                   |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event za pokretanje agenta |
| `--token` | GitHub Personal Access Token           |

---

### mcp

Upravljajte Model Context Protocol (MCP) serverima.

```bash
weapon mcp [command]
```

---

#### add

Dodajte MCP server svojoj konfiguraciji.

```bash
weapon mcp add
```

Ova naredba će vas voditi kroz dodavanje lokalnog ili udaljenog MCP servera.

---

#### list

Navedite sve konfigurirane MCP servere i njihov status veze.

```bash
weapon mcp list
```

Ili koristite kratku verziju.

```bash
weapon mcp ls
```

---

#### auth

Autentifikujte se sa MCP serverom koji je omogućen za OAuth.

```bash
weapon mcp auth [name]
```

Ako ne navedete ime servera, od vas će biti zatraženo da izaberete neki od dostupnih servera koji podržavaju OAuth.
Također možete navesti servere koji podržavaju OAuth i njihov status autentifikacije.

```bash
weapon mcp auth list
```

Ili koristite kratku verziju.

```bash
weapon mcp auth ls
```

---

#### logout

Uklonite OAuth vjerodajnice za MCP server.

```bash
weapon mcp logout [name]
```

---

#### debug

Otklanjanje grešaka (debug) OAuth veze sa MCP serverom.

```bash
weapon mcp debug <name>
```

---

### models

Navedite sve dostupne modele konfiguriranih provajdera.

```bash
weapon models [provider]
```

Ova naredba prikazuje sve modele dostupne kod vaših konfiguriranih provajdera u formatu `provider/model`.
Ovo je korisno za pronalaženje tačnog naziva modela za korištenje u [vašoj konfiguraciji](/docs/config/).
Opciono možete proslijediti ID provajdera za filtriranje modela po tom dobavljaču.

```bash
weapon models anthropic
```

#### Opcije

| Opcija      | Opis                                                                     |
| ----------- | ------------------------------------------------------------------------ |
| `--refresh` | Osvježite keš modela sa models.dev                                       |
| `--verbose` | Koristite detaljniji izlaz modela (uključuje metapodatke poput troškova) |

Koristite `--refresh` zastavicu da ažurirate keširanu listu modela. Ovo je korisno kada su novi modeli dodani provajderu i želite da ih vidite u Weapon.

```bash
weapon models --refresh
```

---

### run

Pokrenite Weapon u neinteraktivnom modu tako što ćete direktno proslijediti prompt.

```bash
weapon run [message..]
```

Ovo je korisno za skriptiranje, automatizaciju ili kada želite brz odgovor bez pokretanja punog TUI-ja. Na primjer:

```bash "weapon run"
weapon run Explain the use of context in Go
```

Također možete priključiti pokrenutu `weapon serve` instancu kako biste izbjegli vrijeme hladnog pokretanja MCP servera pri svakom pokretanju:

```bash
# Start a headless server in one terminal
weapon serve

# In another terminal, run commands that attach to it
weapon run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opcije

| Opcija       | Kratko | Opis                                                                     |
| ------------ | ------ | ------------------------------------------------------------------------ |
| `--command`  |        | Naredba za pokretanje, koristite poruku za argumente                     |
| `--continue` | `-c`   | Nastavite posljednju sesiju                                              |
| `--session`  | `-s`   | ID sesije za nastavak                                                    |
| `--fork`     |        | Forkujte sesiju pri nastavku (koristiti sa `--continue` ili `--session`) |
| `--share`    |        | Podijelite sesiju                                                        |
| `--model`    | `-m`   | Model za korištenje u obliku provider/model                              |
| `--agent`    |        | Agent za korištenje                                                      |
| `--file`     | `-f`   | Fajlovi koje treba priložiti poruci                                      |
| `--format`   |        | Format: default (formatiran) ili json (sirovi JSON događaji)             |
| `--title`    |        | Naslov sesije (koristi skraćeni prompt ako nije navedena vrijednost)     |
| `--attach`   |        | Priključite na pokrenuti Weapon server (npr. http://localhost:4096)    |
| `--port`     |        | Port za lokalni server (zadano na nasumični port)                        |

---

### serve

Pokrenite Weapon headless server za API pristup. Pogledajte [server docs](/docs/server) za kompletan HTTP interfejs.

```bash
weapon serve
```

Ovo pokreće HTTP server koji pruža API pristup funkcionalnosti Weapon-a bez TUI interfejsa. Postavite `WEAPON_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `weapon`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### session

Upravljajte Weapon sesijama.

```bash
weapon session [command]
```

---

#### list

Navedite sve Weapon sesije.

```bash
weapon session list
```

##### Opcije

| Opcija        | Kratko | Opis                                   |
| ------------- | ------ | -------------------------------------- |
| `--max-count` | `-n`   | Ograničenje na N najnovijih sesija     |
| `--format`    |        | Izlazni format: table ili json (table) |

---

### stats

Prikaži statistiku upotrebe tokena i troškova za vaše Weapon sesije.

```bash
weapon stats
```

#### Opcije

| Opcija      | Opis                                                                                                       |
| ----------- | ---------------------------------------------------------------------------------------------------------- |
| `--days`    | Prikaži statistiku za zadnjih N dana (sva vremena)                                                         |
| `--tools`   | Broj alata za prikaz (svi)                                                                                 |
| `--models`  | Prikaži raščlambu korištenja modela (skriveno prema zadanim postavkama). Proslijedite broj za prikaz top N |
| `--project` | Filtriraj po projektu (svi projekti, prazan niz: trenutni projekt)                                         |

---

### export

Izvezite podatke sesije kao JSON.

```bash
weapon export [sessionID]
```

Ako ne unesete ID sesije, od vas će biti zatraženo da odaberete neku od dostupnih sesija.

---

### import

Uvezite podatke sesije iz JSON datoteke ili Weapon dijeljenog URL-a.

```bash
weapon import <file>
```

Možete uvesti iz lokalne datoteke ili Weapon dijeljenog URL-a.

```bash
weapon import session.json
weapon import https://opncd.ai/s/abc123
```

---

### web

Pokrenite Weapon headless server sa web interfejsom.

```bash
weapon web
```

Ovo pokreće HTTP server i otvara web pretraživač za pristup Weapon-u preko web interfejsa. Postavite `WEAPON_SERVER_PASSWORD` da omogućite HTTP osnovnu auth (korisničko ime je zadano na `weapon`).

#### Opcije

| Opcija       | Opis                                                  |
| ------------ | ----------------------------------------------------- |
| `--port`     | Port na kojem treba slušati                           |
| `--hostname` | Hostname na kojem treba slušati                       |
| `--mdns`     | Omogući mDNS otkrivanje                               |
| `--cors`     | Dodatni origin(i) pretraživača koji dozvoljavaju CORS |

---

### acp

Pokrenite ACP (Agent Client Protocol) server.

```bash
weapon acp
```

Ova naredba pokreće ACP server koji komunicira preko stdin/stdout koristeći nd-JSON.

#### Opcije

| Opcija       | Opis                        |
| ------------ | --------------------------- |
| `--cwd`      | Radni direktorij            |
| `--port`     | Port na kojem treba slušati |
| `--hostname` | Hostname na kojem slušati   |

---

### uninstall

Deinstalirajte Weapon i uklonite sve povezane datoteke.

```bash
weapon uninstall
```

#### Opcije

| Opcija          | Kratko | Opis                                          |
| --------------- | ------ | --------------------------------------------- |
| `--keep-config` | `-c`   | Sačuvajte konfiguracijske datoteke            |
| `--keep-data`   | `-d`   | Sačuvajte podatke i snimke sesije             |
| `--dry-run`     |        | Pokažite šta bi bilo uklonjeno bez uklanjanja |
| `--force`       | `-f`   | Preskoči upite za potvrdu                     |

---

### upgrade

Ažurira Weapon na najnoviju verziju ili određenu verziju.

```bash
weapon upgrade [target]
```

Za nadogradnju na najnoviju verziju.

```bash
weapon upgrade
```

Za nadogradnju na određenu verziju.

```bash
weapon upgrade v0.1.48
```

#### Opcije

| Opcija     | Kratko | Opis                                                    |
| ---------- | ------ | ------------------------------------------------------- |
| `--method` | `-m`   | Korišteni način instalacije; curl, npm, pnpm, bun, brew |

---

## Globalne opcije

Weapon CLI prihvata sljedeće globalne zastavice.

| Opcija         | Kratko | Opis                                      |
| -------------- | ------ | ----------------------------------------- |
| `--help`       | `-h`   | Prikaži pomoć                             |
| `--version`    | `-v`   | Ispiši broj verzije                       |
| `--print-logs` |        | Ispis logova u stderr                     |
| `--log-level`  |        | Nivo logovanja (DEBUG, INFO, WARN, ERROR) |

---

## Varijable okruženja

Weapon se može konfigurirati pomoću varijabli okruženja.

| Varijabla                             | Tip     | Opis                                                               |
| ------------------------------------- | ------- | ------------------------------------------------------------------ |
| `WEAPON_AUTO_SHARE`                 | boolean | Automatski dijeli sesije                                           |
| `WEAPON_GIT_BASH_PATH`              | string  | Putanja do Git Bash izvršne datoteke na Windows-u                  |
| `WEAPON_CONFIG`                     | string  | Putanja do konfiguracijskog fajla                                  |
| `WEAPON_CONFIG_DIR`                 | string  | Putanja do konfiguracijskog direktorija                            |
| `WEAPON_CONFIG_CONTENT`             | string  | Inline json konfiguracijski sadržaj                                |
| `WEAPON_DISABLE_AUTOUPDATE`         | boolean | Onemogući automatske provjere ažuriranja                           |
| `WEAPON_DISABLE_PRUNE`              | boolean | Onemogući brisanje (pruning) starih podataka                       |
| `WEAPON_DISABLE_TERMINAL_TITLE`     | boolean | Onemogući automatsko ažuriranje naslova terminala                  |
| `WEAPON_PERMISSION`                 | string  | Inline json konfiguracija dozvola                                  |
| `WEAPON_DISABLE_DEFAULT_PLUGINS`    | boolean | Onemogući podrazumijevane dodatke (plugins)                        |
| `WEAPON_DISABLE_LSP_DOWNLOAD`       | boolean | Onemogući automatsko preuzimanje LSP servera                       |
| `WEAPON_ENABLE_EXPERIMENTAL_MODELS` | boolean | Omogući eksperimentalne modele                                     |
| `WEAPON_DISABLE_AUTOCOMPACT`        | boolean | Onemogući automatsko sažimanje konteksta                           |
| `WEAPON_DISABLE_CLAUDE_CODE`        | boolean | Onemogući čitanje iz `.claude` (prompt + vještine)                 |
| `WEAPON_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Onemogući čitanje `~/.claude/CLAUDE.md`                            |
| `WEAPON_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Onemogući učitavanje `.claude/skills`                              |
| `WEAPON_DISABLE_MODELS_FETCH`       | boolean | Onemogući dohvaćanje modela iz udaljenih izvora                    |
| `WEAPON_FAKE_VCS`                   | string  | Lažni VCS provajder za potrebe testiranja                          |
| `WEAPON_DISABLE_FILETIME_CHECK`     | boolean | Onemogući provjeru vremena datoteke radi optimizacije              |
| `WEAPON_CLIENT`                     | string  | Identifikator klijenta (zadano na `cli`)                           |
| `WEAPON_ENABLE_EXA`                 | boolean | Omogući Exa alate za web pretraživanje                             |
| `WEAPON_SERVER_PASSWORD`            | string  | Omogući osnovnu autentifikaciju za `serve`/`web`                   |
| `WEAPON_SERVER_USERNAME`            | string  | Poništi osnovno korisničko ime autentifikacije (zadano `weapon`) |
| `WEAPON_MODELS_URL`                 | string  | Prilagođeni URL za dohvaćanje konfiguracije modela                 |

---

### Eksperimentalno

Ove varijable okruženja omogućavaju eksperimentalne karakteristike koje se mogu promijeniti ili ukloniti.

| Varijabla                                       | Tip     | Opis                                              |
| ----------------------------------------------- | ------- | ------------------------------------------------- |
| `WEAPON_EXPERIMENTAL`                         | boolean | Omogući sve eksperimentalne funkcije              |
| `WEAPON_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Omogući otkrivanje ikona                          |
| `WEAPON_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Onemogući kopiranje pri odabiru u TUI             |
| `WEAPON_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Zadano vremensko ograničenje za bash naredbe u ms |
| `WEAPON_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Maksimalni izlazni tokeni za LLM odgovore         |
| `WEAPON_EXPERIMENTAL_FILEWATCHER`             | boolean | Omogući praćenje datoteka za cijeli direktorij    |
| `WEAPON_EXPERIMENTAL_OXFMT`                   | boolean | Omogući oxfmt formatter                           |
| `WEAPON_EXPERIMENTAL_LSP_TOOL`                | boolean | Omogući eksperimentalni LSP alat                  |
| `WEAPON_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Onemogući praćenje datoteka                       |
| `WEAPON_EXPERIMENTAL_EXA`                     | boolean | Omogući eksperimentalne Exa funkcije              |
| `WEAPON_EXPERIMENTAL_LSP_TY`                  | boolean | Omogući eksperimentalnu provjeru tipa LSP         |
| `WEAPON_EXPERIMENTAL_MARKDOWN`                | boolean | Omogući eksperimentalne Markdown funkcije         |
| `WEAPON_EXPERIMENTAL_PLAN_MODE`               | boolean | Omogući Plan mod                                  |
